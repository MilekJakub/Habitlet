import { LogoText } from "@/components/typography/logo-text";
import React from "react";

export const DevPage = () => {
  return (
    <div>
      <LogoText>DevPage</LogoText>
    </div>
  );
};

export default DevPage;
